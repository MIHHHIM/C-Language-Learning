#include <stdio.h>
#include <stdbool.h>

int main() 
{
    bool a = 0;
    bool b = 100;
    bool c = -50;
    bool d = true;

    printf("a (0) -> %d\n", a);
    printf("b (100) -> %d\n", b);
    printf("c (-50) -> %d\n", c);
    printf("d (true) -> %d\n", d);

    return 0;
}